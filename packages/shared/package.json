{
  "name": "@pcf/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Спільні TypeScript-типи для PCF-калькулятора (Material, EmissionFactor, PCFCalculation, enums).",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch",
    "clean": "rimraf dist *.tsbuildinfo"
  },
  "devDependencies": {
    "rimraf": "^5.0.10",
    "typescript": "^5.6.3"
  }
}
